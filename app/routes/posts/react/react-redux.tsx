import React from "react";
import MD_ReactRedux from "~/posts/react/react-redux.md";

type ReactReduxPropsType = {};

const ReactRedux: React.FC<ReactReduxPropsType> = ({}) => {
  return (
    <div data-test-id="react_redux">
      <MD_ReactRedux />
    </div>
  );
};

export default ReactRedux;
